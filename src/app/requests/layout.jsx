export const metadata = {
  title: 'Requests | Thirsty for Knowledge',
  description: 'Ask for what you want to read next, and vote on ideas.',
};

export default function RequestsLayout({ children }) {
  return children;
}
